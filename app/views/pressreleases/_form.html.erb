<%= form_for(@pressrelease) do |f| %>
  <% if @pressrelease.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pressrelease.errors.count, "error") %> prohibited this document from being saved:</h2>

      <ul>
      <% @pressrelease.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.file_field :file %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
